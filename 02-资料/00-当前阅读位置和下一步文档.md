# 00-当前阅读位置和下一步文档

这个文件只回答一个问题：

我现在该看哪份官方文档，看完后做什么。

## 当前读到哪里

当前阶段：Camera + OpenCV + RKNN 汇合整理

当前状态：

- Buildroot 快速体验手册已经完成第一轮。
- 板子已完成串口、WiFi、SSH、Camera 屏幕出图。
- 已经从 `/dev/video0` 抓到一帧 `1280x720 NV12`。
- 已经在 Windows 用 `ffplay` 正常查看 NV12 原始帧。
- 已读 `基于Buildroot系统Camera应用开发手册V1.1.pdf`，确认该文档价值有限，主要是 Qt/OpenCV/V4L2 简单示例索引。
- 已读 `OpenCV4使用手册V1.0.pdf`，确认出厂 Buildroot 已内置 OpenCV 4.5.5。
- 已验证板端 Python `cv2` 可用。
- 已验证 `cv2.VideoCapture(0)` 和强制 V4L2 后端打开失败。
- 已通过显式 GStreamer pipeline 跑通 `/dev/video0 -> OpenCV -> JPG`。
- 正在阅读 `AI例程测试手册V1.0.pdf`，目标是先跑最小 `01_lenet`，不直接上 YOLO。
- 板端已确认 `cv2`、`numpy` 存在。
- 已安装 `rknn-toolkit-lite2-1.5.0`，`rknnlite` 可正常导入。
- 已将板端 `rknn_server/librknnrt.so` 从 1.3.0 更新到 1.5.0，旧版本已备份。
- 已跑通官方最小 `01_lenet`，输出 `[5]: 1.0`。
- 已整理 [[Camera-OpenCV-RKNN汇合路线]]。
- 已判断下一步优先验证 `11_facedet_scrfd_npu`，因为它最适合验证 Camera 实时帧进入 RKNN。
- 已读 AI 手册第 6 章，并跑通改造后的 `11_facedet_scrfd_npu` 单帧人脸检测。
- 检测结果为 `faces 1`，结果图已保存到 `E:\RK3568\face_stage\result\scrfd_result_rotated.jpg`。
- 已跑通 SCRFD 实时 90 帧检测，输出 `fps=6.798`，结果图 `E:\RK3568\face_stage\result\scrfd_realtime_last.jpg`。
- 当前决策：整理优先级最高。后续先收敛实验目录和记录，再继续跑 YOLO、实时显示或推流。
- 已新增 [[附录-实验产物索引]]，统一登记脚本、测试图片、板端路径、Windows/WSL 路径。

现在不要做：

- 不要继续盲跑 Camera 命令。
- 不要直接跑 YOLO。
- 不要看 Linux5.10 AI 文档。
- 不要继续堆临时脚本。

固定规则：

```text
每成功一个功能，必须同步文档。
```

同步范围：

- 每日进度记录。
- 对应专项笔记。
- [[附录-实验产物索引]]。
- WSL 项目实验目录 README。

## 当前文档结论

`01、测试文档/03【正点原子】ATK-DLRK3568_AI例程测试手册V1.0.pdf`

已经完成第一轮阅读和最小例程验证。

结论：

- 第 1 章是开发环境说明，当前不用细看。
- 第 2 章是更新板端 RKNN 运行库，涉及 `librknnrt.so` 和 `rknn_server`。
- 第 3 章是宿主机模型转换环境，当前不做模型转换，暂时只知道用途。
- 第 4 章是 CPU/NPU 定频，首轮跑通例程前不用深入。
- 第 5 章是当前核心，尤其是 `5.1` 安装 RKNN Toolkit Lite2 和 `5.3` 跑 `01_lenet`。
- 第 6 章以后是复杂例程，等 `01_lenet` 跑通后再看。

## 下一步

不要继续往 YOLO 跳。

AI 手册第 6 章已经完成第一轮验证。

重点看：

- 回看第 6.2 节：官方例程默认使用 `cv2.VideoCapture(0)` 和屏幕显示。
- 对照当前改造：使用 GStreamer pipeline，先保存单帧 JPG。
- 注意图像方向：必须旋转后再送入 SCRFD，否则可能检测不到人脸。

看完后再做：

- 先整理 Python 实验项目目录。
- 补齐 README。
- 把已验证脚本移入实验目录。
- 记录板端运行路径和结果图片。
- 更新 [[附录-实验产物索引]]。
- 整理完再决定是否做实时 SCRFD 或 YOLO。

当前 WSL 项目目录：

```text
\\wsl$\Ubuntu-20.04\home\rk3568\work\rk3568_ai_camera
```

当前实验目录：

```text
experiments/opencv_camera
experiments/scrfd_face
```

代码仓库：

```text
https://github.com/zhaoxl12138/rk3568_ai_camera
```

暂时不用看：

- Linux5.10 AI 例程。
- YOLO 类复杂例程。
- 自己转换模型。
- RKNN Toolkit 训练/量化细节。

## 跟跑规则

每进入一个测试前，先看对应文档，建立基本地图；然后再跟着 Codex 跑命令验证。

固定节奏：

1. 先说明当前阶段要解决什么问题。
2. 指定要看的官方文档。
3. 只圈出当前要看的章节或关键词，不要求整本看完。
4. 看完后再一起跑命令或例程验证。
5. 最后把命令、现象、原因记录到 Obsidian。

Codex 后续带测时，每次都要明确说明：

- 当前在测什么。
- 测试前先看哪份文档。
- 只需要重点看哪几节。
- 暂时不用看哪些内容。
- 看完文档后再执行哪些命令。

## 文档队列

| 顺序 | 文档 | 状态 | 用途 |
|---|---|---|---|
| 1 | `01、测试文档/01【正点原子】ATK-DLRK3568_Buildroot系统快速体验手册V1.3.pdf` | 已完成第一轮 | 确认出厂 Buildroot、外设、网络、Camera |
| 2 | `03、辅助文档/30【正点原子】基于Buildroot系统Camera应用开发手册V1.1.pdf` | 已读 | 对齐 Camera/V4L2 出图流程 |
| 3 | `03、辅助文档/19【正点原子】ATK-DLRK3568_OpenCV4使用手册V1.0.pdf` | 已读并验证 | Camera 帧进入 OpenCV |
| 4 | `01、测试文档/03【正点原子】ATK-DLRK3568_AI例程测试手册V1.0.pdf` | 下一阶段 | 官方 AI 例程和 RKNN/NPU |
| 5 | `03、辅助文档/18【正点原子】ATK-DLRK3568_RTMP推流V1.1.pdf` | 推流阶段 | 先参考 RTMP，再扩展 RTSP |

## 暂时不看

- Android 系列
- Qt 系列
- OpenHarmony 系列
- Linux5.10 AI 例程
- Buildroot SDK 编译手册
- 交叉编译器安装手册

原因：

当前还在 Camera/V4L2 出图阶段，这些资料会分散注意力。

## 附录入口

- [[附录-资料汇总PDF注意事项]]
- [[附录-正点原子资料版本更新记录解读]]
- [[附录-实验产物索引]]

#当前阅读 #资料 #Camera #V4L2 #RKNN #整理
