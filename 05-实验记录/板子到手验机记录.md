# 板子到手验机记录

## 当前结论

RK3568 开发板已经完成最小验机，出厂 Buildroot 系统可以正常启动，串口、WiFi、SSH 和屏幕 Camera 出图链路已经打通。

## 已确认信息

- 串口：Type-C UART
- 串口设备：`COM3 USB-Enhanced-SERIAL CH343`
- 串口登录用户：`root`
- 当前固定 SSH 地址：`root@192.168.0.230`
- 曾用 DHCP 地址：`root@192.168.0.103`、`root@192.168.0.102`
- 系统：`Buildroot 2018.02-rc3`
- 内核：`Linux 4.19.232`
- 架构：`aarch64`
- 主机名：`ATK-DLRK3568`
- 内存：约 `3.6GiB`
- rootfs：约 `5.9G`
- userdata：约 `52G`

## 验机结果

### 2026-05-28

- 串口登录正常。
- 出厂 Buildroot 4.19 系统正常。
- WiFi 和 SSH 正常，SSH 地址为 `root@192.168.0.103`。
- 屏幕 Camera 出图正常。
- V4L2 主采集节点确认为 `/dev/video0` / `/dev/video-camera0`。
- 主采集默认格式为 `1280x720 NV12`。
- 主采集支持最大范围为 `3840x2160`。
- 命令行抓帧正常，文件为 `/tmp/frame_1280x720_nv12.yuv`。
- Windows `ffplay` 可以正常显示 NV12 原始帧。

### 2026-05-30

- 确认板端 WiFi 由 ConnMan 管理，不适合直接改 `/etc/network/interfaces`。
- 确认 `192.168.0.103` 已有设备响应，不能继续作为固定 IP。
- 保留 ConnMan/DHCP 负责 WiFi 连接。
- 新增 `/etc/init.d/S99wlan0-static-alias`，在 `wlan0` 上追加固定别名 IP `192.168.0.230/24`。
- 重启验证通过：开机后 `wlan0` 同时存在 DHCP 地址和固定别名地址 `192.168.0.230/24`。
- Windows 侧 `ping 192.168.0.230` 正常。

## 关键命令

固定访问地址：

```text
root@192.168.0.230
```

抓取单帧：

```bash
v4l2-ctl -d /dev/video0 \
  --set-fmt-video=width=1280,height=720,pixelformat=NV12 \
  --stream-mmap=3 \
  --stream-count=1 \
  --stream-to=/tmp/frame_1280x720_nv12.yuv

ls -lh /tmp/frame_1280x720_nv12.yuv
```

抓帧成功后文件大小应约为：

```text
1280 * 720 * 1.5 = 1382400 bytes
```

如果 `v4l2-ctl` 不存在：

```bash
find / -name "v4l2-ctl" 2>/dev/null
```

## 关联

- [[下一步任务看板]]
- [[每日进度记录]]
- [[V4L2命令行抓帧记录]]
- [[00-AI视觉系统主线]]
- [[RK3568 YOLOv8n AI Camera项目]]

#验机 #Buildroot #Camera #SSH #串口
