# 每日进度记录

## 2026-05-28

- 完成 RK3568 开发板上电后的最小验机。
- 通过 Type-C UART 串口登录 root。
- 确认串口设备为 `COM3 USB-Enhanced-SERIAL CH343`。
- 确认板端系统为 `Buildroot 2018.02-rc3`。
- 确认内核为 `Linux 4.19.232`，架构为 `aarch64`。
- 确认 rootfs、userdata、内存状态正常。
- 接好 WiFi 天线，完成网络连接。
- 通过 SSH 登录开发板：`root@192.168.0.103`。
- 确认屏幕上的 Camera 可以正常出图。
- 枚举 `/dev/video*` 和 `/dev/media*`，确认存在 `/dev/video0` 到 `/dev/video8`、`/dev/video-camera0`、`/dev/media0`。
- 使用 `v4l2-ctl --list-devices` 查看 Camera/V4L2 拓扑。
- 初步确认 `rkisp_mainpath` 对应 `/dev/video0`，`rkisp_selfpath` 对应 `/dev/video1`。
- 确认 `/dev/video0` 与 `/dev/video-camera0` 均为 `rkisp_mainpath`。
- 确认主采集默认格式为 `1280x720 NV12`，单帧大小 `1382400` 字节。
- 确认主采集支持 `UYVY/NV16/NV12/NV21/YU12/Bayer RAW` 等格式，范围 `32x16` 到 `3840x2160`。
- `ps | grep -iE "camera|rk|isp|video|qt|gst|opencv"` 暂未发现明显 Camera demo 进程。
- 首次抓帧时因屏幕相机软件占用 `/dev/video0`，出现 `Device or resource busy`，生成 0 字节文件。
- 关闭屏幕相机软件后，使用 `v4l2-ctl --stream-mmap` 从 `/dev/video0` 成功抓取一帧。
- 抓帧文件：`/tmp/frame_1280x720_nv12.yuv`，大小 `1.4M`。
- 将 NV12 原始帧取回 Windows，并用 `ffplay` 正常显示画面。
- 详细解释记录见 [[V4L2命令行抓帧记录]]。
- 开始推进官方 AI 例程，选择最小 `01_lenet` 验证 RKNN Lite/NPU 推理闭环。
- 将官方 `01_lenet` 例程复制到 `E:\RK3568\official_ai_examples\01_lenet`，便于上传到板端。
- 从官方 `rknn-toolkit2-1.5.0.zip` 中提取 RKNN Toolkit Lite2 的 aarch64 wheel，作为板端缺少 `rknnlite` 时的备用安装包。
- 从官方 `rknpu2-1.5.0.zip` 中提取 RK356X Linux 版 `librknnrt.so`，作为运行库缺失时的备用文件。
- 新增记录见 [[官方AI例程运行记录]]。
- 纠正学习节奏：后续改为先看对应文档，再一起跑命令验证，不再直接跳到下一大阶段。
- 重构 Obsidian 笔记架构：建立 [[00-RK3568学习主入口]] 和 [[00-当前阅读位置和下一步文档]] 两个核心入口，将资料汇总和版本记录降级为附录。
- 继续收敛笔记层级：将 [[V4L2命令行抓帧记录]] 和 [[官方AI例程运行记录]] 移入 [[00-专项笔记入口]]，避免细节笔记和主架构同级展示。
- 将资料附录改为中文命名，建立 [[00-附录入口]]，移除 `reference-` 命名。
- 合并 `01-主线` 中的路线、阶段任务、技术栈三个文件，统一为 [[00-AI视觉系统主线]]，避免主线目录冗余。
- 将资料入口改名为 [[00-当前阅读位置和下一步文档]]，强化“当前正在看什么、看完做什么”的第一屏信息。

## 2026-05-30

- 阅读 `基于Buildroot系统Camera应用开发手册V1.1.pdf`，确认该文档主要是 Qt/OpenCV/V4L2 简单应用示例，对当前项目有参考但不是核心教程。
- 扫描 A 盘基础资料目录和主要 README/TXT，确认 A 盘本质是资料仓库，不是线性学习教程。
- 新增 [[附录-A盘基础资料目录解读]]，记录 A 盘各目录用途、当前优先级和后续查阅策略。
- 创建 GitHub 私有仓库 `zhaoxl12138/RK3568`，用于同步当前 Obsidian 学习笔记。
- 通过串口确认板端 WiFi 由 ConnMan 管理，不适合直接改 `/etc/network/interfaces`。
- 尝试 ConnMan 静态配置失败后已回滚，最终采用更稳的方案：保留 ConnMan/DHCP，新增 `/etc/init.d/S99wlan0-static-alias` 给 `wlan0` 追加固定别名 IP `192.168.0.230/24`。
- 已重启验证：板子开机后自动拥有 `192.168.0.230`，Windows 侧 ping 正常。
- 使用 `v4l2-ctl` 从 `/dev/video0` 连续抓取 30 帧 `1280x720 NV12`，生成文件 `/tmp/frame_1280x720_30frames_nv12.yuv`。
- 确认 30 帧文件大小为 `41472000` 字节，计算方式为 `1280 * 720 * 1.5 * 30`，说明连续采集稳定。
- 使用 `v4l2-ctl` 从 `/dev/video0` 抓取 1 帧 `1920x1080 NV12`，生成文件 `/tmp/frame_1920x1080_1frame_nv12.yuv`。
- 确认 1080P 单帧文件大小为 `3110400` 字节，计算方式为 `1920 * 1080 * 1.5`，说明主采集节点支持 1080P NV12。
- Camera 命令行采集阶段暂时完成，下一步进入 OpenCV 阶段：先阅读 OpenCV4 使用手册，再验证 OpenCV 读取 `/dev/video0`。
- 阅读 `OpenCV4使用手册V1.0.pdf`，确认出厂 Buildroot 已内置 OpenCV 4.5.5，Python 可直接调用 `cv2`。
- 板端确认 `python3` 版本为 3.8.6，`cv2.__version__` 为 4.5.5。
- 测试 `cv2.VideoCapture('/dev/video0')` 失败，OpenCV 默认 GStreamer 管线无法自动打开当前 RKISP Camera。
- 测试 `cv2.VideoCapture(0, cv2.CAP_V4L2)` 失败，说明 OpenCV 默认 V4L2 打开方式不适合当前 RKISP mplane 节点。
- 使用显式 GStreamer pipeline 跑通 OpenCV 读取 Camera：`v4l2src device=/dev/video0 ! video/x-raw,format=NV12,width=1280,height=720 ! videoconvert ! video/x-raw,format=BGR ! appsink`。
- 成功生成 `/tmp/opencv_frame_gst.jpg`，文件大小约 `266K`，图像为 `1280x720` JPEG。
- 将测试图片取回 Windows：`E:\RK3568\opencv_test\opencv_frame_gst.jpg`，确认画面正常。
- 将成功的 OpenCV Camera 验证脚本固化到 WSL 工作目录：`\\wsl$\Ubuntu-20.04\home\rk3568\work\rk3568_ai_camera\experiments\opencv_camera\opencv_capture_gst.py`。
- 新增 [[OpenCV读取Camera记录]]，记录 OpenCV 默认失败原因和成功 pipeline。
- 阅读 `AI例程测试手册V1.0.pdf`，确认当前只需要看第 2 章、第 5.1 节、第 5.3 节，暂时不看 YOLO、模型转换和 Linux5.10 AI 文档。
- 对照官方 `01_lenet` 源码，确认最小例程依赖 `cv2`、`numpy`、`rknnlite`、`LeNet5_mnist_model.rknn` 和 `5.png`。
- 串口检查板端 Python 依赖：`cv2=True`、`numpy=True`、`rknnlite=False`。
- 串口检查板端 RKNN 运行库：`rknn_server`、`start_rknn.sh`、`restart_rknn.sh`、`librknnrt.so` 均存在，但版本为 `1.3.0`。
- 确认资料包内有 `rknn_toolkit_lite2-1.5.0-cp38-cp38-linux_aarch64.whl`，以及 RK356X Linux aarch64 的 `librknnrt.so/rknn_server` 1.5.0 版本。
- 将 `rknn_toolkit_lite2` wheel、1.5.0 RKNN 运行库和 `01_lenet` 例程整理到 Windows 临时目录 `E:\RK3568\ai_stage`。
- 通过临时 HTTP 服务把文件传到板端 `/userdata/ai_stage` 和 `/userdata/aidemo/01_lenet`。
- 备份板端旧 RKNN 1.3.0 运行库到 `/userdata/rknn_backup_1.3.0`。
- 将板端 `/usr/lib/librknnrt.so`、`/usr/bin/rknn_server`、`start_rknn.sh`、`restart_rknn.sh` 更新为资料包 1.5.0 版本。
- 安装 `rknn-toolkit-lite2-1.5.0` 成功，确认 `from rknnlite.api import RKNNLite` 正常。
- 执行 `restart_rknn.sh` 后确认 `rknn_server` 版本为 `1.5.0`，`librknnrt.so` 版本为 `1.5.0`，NPU 驱动版本为 `0.8.2`。
- 在 `/userdata/aidemo/01_lenet` 执行 `python3 atk_lenet_demo.py` 成功，输出 `[5]: 1.0`。
- 官方最小 RKNN 推理闭环跑通：`Python -> rknnlite -> .rknn模型 -> RKNN Runtime -> NPU -> 推理结果`。
- 整理 [[Camera-OpenCV-RKNN汇合路线]]，明确后续项目路线为 `Camera -> OpenCV Mat -> 前处理 -> RKNN 推理 -> 后处理 -> 显示/推流`。
- 扫描官方 AI 例程目录，确认 `11_facedet_scrfd_npu`、`12_pp_human_seg_npu`、`10_carseg_yolop_npu/cam.py` 都是 Camera 实时例程，但源码默认使用 `cv2.VideoCapture(0)`。
- 结合当前板端实测结果，判断官方实时 AI 例程不能原样运行，需要改成已验证的 GStreamer pipeline。
- 确定下一步优先跑 `11_facedet_scrfd_npu`，因为它模型较小、源码较短、能验证目标检测画框和关键点。
- 读完 AI 例程测试手册第 6 章，开始验证 `11_facedet_scrfd_npu`。
- 将官方 `scrfd.rknn` 和改造后的 `main_gst_one_frame.py` 上传到板端 `/userdata/aidemo/11_facedet_scrfd_npu`。
- 第一次单帧 SCRFD 检测链路跑通，但未旋转图像，结果为 `faces 0`，原因是 Camera 画面方向横置。
- 补回官方源码中的 `cv2.ROTATE_90_COUNTERCLOCKWISE` 旋转后再次运行，输出 `faces 1`。
- 成功保存人脸检测结果图 `/tmp/scrfd_result.jpg`，并取回 Windows：`E:\RK3568\face_stage\result\scrfd_result_rotated.jpg`。
- 当前关键链路已跑通：`Camera -> GStreamer -> OpenCV Mat -> SCRFD RKNN -> 后处理 -> 人脸框/5点关键点 -> JPG`。
- 明确后续决策：整理优先级最高，先收敛 Python 实验项目目录和记录，不继续堆临时脚本。
- 在 WSL 工作目录建立项目实验仓库骨架：`\\wsl$\Ubuntu-20.04\home\rk3568\work\rk3568_ai_camera`。
- 新增实验目录说明，并整理 `opencv_camera` 与 `scrfd_face` 两个已验证实验。
- 新增 [[附录-实验产物索引]]，统一登记测试脚本、测试图片、板端路径、Windows 临时目录和 WSL 项目路径。
- 将关键测试图片复制进 Obsidian 仓库内部：`opencv_frame_gst.jpg` 和 `scrfd_result_rotated.jpg`，便于后续 GitHub 同步和回顾。
- 创建代码私有仓库 `zhaoxl12138/rk3568_ai_camera`，用于同步 WSL 中的 AI Camera 实验项目代码。
- 将 WSL 项目目录 `/home/rk3568/work/rk3568_ai_camera` 初始化为 Git 仓库并推送到 GitHub，默认分支为 `main`。
- 代码仓库当前包含 `opencv_camera` 和 `scrfd_face` 两个已验证实验，以及功能完成记录规则。
- 新增 `main_gst_realtime.py`，在板端跑通 SCRFD 实时 90 帧测试。
- 实时测试输出：`processed_frames=90`、`fps=6.798`、`avg_faces_per_frame=0.889`。
- 生成并取回结果图：`/tmp/scrfd_realtime_last.jpg` -> `E:\RK3568\face_stage\result\scrfd_realtime_last.jpg`。
- 将 `scrfd_realtime_last.jpg` 同步到 Obsidian：`08-附录/实验产物/assets/scrfd_realtime_last.jpg`。
- 阅读并整理 SCRFD 单帧脚本和实时脚本，确认两者重复了 Camera 打开、模型加载、前处理、推理和后处理代码。
- 将 WSL 项目中的 SCRFD 公共逻辑拆分为 `camera_gst.py` 和 `scrfd_detector.py`，入口脚本保留为 `main_gst_one_frame.py` 和 `main_gst_realtime.py`。
- WSL 本地执行 `py_compile` 语法检查通过。
- 由于 Codex 无法直接通过 SSH 登录板端，本次拆分后的脚本仍需在 MobaXterm 中手动复制或下载后复测。
- 在板端通过 `--display` 参数跑通 MIPI 屏实时显示：`python3 main_gst_realtime.py --frames 300 --save-every 60 --display`。
- MIPI 屏实时显示 Camera 画面与 SCRFD 人脸检测框，说明 `cv2.imshow()` 在当前 Buildroot + MIPI 屏环境可用。
- 本次 MIPI 屏测试结果：`processed_frames=300`、`elapsed_sec=45.322`、`fps=6.619`、`avg_faces_per_frame=0.913`。
- 当前已跑通更完整的感官闭环：`Camera -> GStreamer -> OpenCV -> SCRFD RKNN -> MIPI屏实时显示`。
- 新增 [[MIPI屏显示链路]]，梳理 `cv2.imshow()`、MIPI 屏、framebuffer、DRM/KMS、Qt 在当前项目中的关系。
- 建立 C++17/CMake 项目骨架，固定 `CameraSource`、`Detector`、`DisplaySink`、`StreamSink` 和 `AppPipeline` 模块边界。
- 新增代码仓库文档 `docs/C++17-CMake项目骨架设计.md`，记录骨架目录、职责和后续接入顺序。
- 在 WSL Ubuntu 20.04 中安装 `build-essential` 和 `cmake`。
- 编译并运行 C++17/CMake stub 工程成功，输出 `processed_frames 1`。
- C++ 工程骨架阶段完成第一轮验证，下一步接入 OpenCV C++。
- 在 WSL Ubuntu 20.04 中安装 `libopencv-dev`。
- CMake 已找到 OpenCV 4.2.0，并成功编译链接 OpenCV C++ 模块。
- 新增 `OpenCvCameraSource` 和 `OpenCvDisplaySink`，当前已编译通过，主程序仍默认运行 stub pipeline。

## 2026-05-25

- 确定 RK3568 AI视觉系统工程路线：Camera -> Linux -> 视频流 -> AI推理 -> RTSP/RTMP -> 边缘部署。
- 创建并整理 Obsidian 仓库：`E:\obsidian_github\RK3568`。
- 建立学习总览、路线、资料阅读顺序、环境注意事项、项目目标和任务看板。
- 阅读并整理 `RK3568资料汇总.pdf` 的重点注意事项。
- 整理正点原子资料版本更新记录，确认 Camera、AI、Linux5.10、Buildroot 文档的演进。
- 安装 WSL2 Ubuntu 20.04 到 `E:\wsl\Ubuntu-20.04`。
- 设置默认用户 `rk3568` 和工作目录 `/home/rk3568/work`。
- 整理 VS Code + WSL 插件的使用方式。

## 关联

- [[下一步任务看板]]
- [[板子到手验机记录]]
- [[官方AI例程运行记录]]
- [[00-RK3568学习主入口]]

#进度 #日报 #任务
