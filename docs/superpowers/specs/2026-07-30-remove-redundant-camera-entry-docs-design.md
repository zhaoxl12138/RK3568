# 删除 Camera 重复入口文档设计

## 1. 问题

`06-任务/RK3568-Camera-Sensor从零点亮路线.md` 和
`06-任务/RK3568-Camera驱动学习总入口.md`
与周计划、章节文档重复，导致 Obsidian 文件树出现“入口套入口”，用户难以判断应该打开哪个文件。

## 2. 目标结构

`06-任务` 中 Camera 求职学习只保留三种职责：

```text
Camera驱动求职第1周执行计划
→ 唯一执行入口：当前章节、每日任务、岗位化学习规则

Camera驱动第1章、Camera驱动第2章……
→ 技术知识、用户回答、正确答案、源码和板端证据

assets
→ 章节引用的原理图图片
```

`01-下一步任务看板.md` 继续保留全仓库阶段状态，不承接 Camera 章节技术内容。

## 3. 内容迁移

删除前只迁移两类仍有价值的信息：

1. 将总入口中的“当前章节状态”和“概念、证据、故障、表达”四类面试出题规则并入第 1 周执行计划。
2. 将从零点亮路线中的“新 Sensor 前置资料与接口检查清单”压缩后并入第一章第 8 节。

以下重复内容不迁移：

- Sensor、D-PHY、RKISP、V4L2 的通用介绍。
- 六步点亮流程。
- 黑屏排查流程。
- 最终面试模板。

这些内容已经存在于第一章、第二章、周计划的 Day 3～Day 7 和第一章岗位化复盘题中。

## 4. 删除和引用更新

删除：

```text
06-任务/RK3568-Camera-Sensor从零点亮路线.md
06-任务/RK3568-Camera驱动学习总入口.md
00-首页/学习驾驶舱/pages/notes/06-任务--RK3568-Camera-Sensor从零点亮路线.html
00-首页/学习驾驶舱/pages/notes/06-任务--RK3568-Camera驱动学习总入口.html
```

更新：

- 删除第一章顶部的总入口反向链接。
- 删除第 1 周执行计划顶部和固定资料入口中的旧链接。
- 从网页 `vault-data.js` 和 `pages/notes.html` 中移除两个已删除文档的目录项。

本次不运行完整网页生成器。原因是第一章新增的面试复盘题仍在 Obsidian 作答阶段，完整重建会提前把空答案发布到 HTML。网页目录采用可验证的机械删除；未来重新生成时，由于两个 Markdown 已删除，它们不会重新出现。

## 5. 验收标准

- `06-任务` 文件树不再出现两个旧文件。
- 全仓库活动源码中不再引用两个旧标题或 HTML 文件名。
- 网页目录不再展示两个旧页面。
- 两个旧 HTML 文件不存在。
- 第一章保留新 Sensor 前置检查清单。
- 第 1 周执行计划成为 Camera 学习唯一执行入口。
- Markdown 链接检查三类错误均为 0。
- 网页测试全部通过。
