# 2026-07-28｜Camera 驱动 Day 1 验收

## 审批结论

**有条件通过。** RK3568 Camera 驱动链路的关键板端证据已经具备，但 `dmesg` 命令因引号未闭合没有真正执行，需要补采一次日志。

## 已确认事实

### 系统

- 主机名：`ATK-DLRK3568`
- 内核：`Linux 4.19.232`
- 架构：`aarch64`
- 内核编译时间：`2025-01-15`

### 视频节点

- `/dev/video0` ～ `/dev/video8` 均存在。
- `/dev/video-camera0` 是 `/dev/video0` 的符号链接。
- `rkisp_mainpath` 对应 `/dev/video0` ～ `/dev/video6`。
- `rkisp-statistics` 对应 `/dev/video7`、`/dev/video8`。
- 主采集节点 `/dev/video0` 的驱动名为 `rkisp_v5`。

### 当前输出格式

- 分辨率：`3840x2160`
- 像素格式：`NV12`
- 多平面视频采集：`Video Capture Multiplanar`
- 当前裁剪区域：`3840x2160`
- Sensor 链路像素格式：`SGBRG10_1X10`

### Media Graph

板端已经给出了完整链路：

```text
m00_b_imx415 4-001a-1
    -> rockchip-csi2-dphy0
    -> rkisp-csi-subdev
    -> rkisp-isp-subdev
    -> rkisp_mainpath (/dev/video0)
```

关键参数：

- Sensor 挂在 I2C 总线 `4`，地址为 `0x1a`。
- Sensor 输出 `SGBRG10_1X10`，原始尺寸 `3864x2192`。
- CSI/ISP 当前裁剪为 `3840x2160`。
- Media Graph 显示链路均为 `ENABLED`。
- Sensor 到 CSI 的帧率标记为 `10000/300000`，即约 `30 fps`。
- CSI/ISP 已将 RAW10 Bayer 数据转换为 `/dev/video0` 的 NV12 输出。

## 你的 Day 1 技术结论

目前可以确认：

1. IMX415 已被 Linux 驱动识别并注册为 V4L2 Sensor subdev。
2. IMX415 已经通过 MIPI CSI-2 D-PHY 接入 RK3568。
3. CSI、ISP 和主采集节点之间的 Media Controller 链路已经建立。
4. `/dev/video0` 不是 Sensor 原始节点，而是经过 RKISP 主路径输出的 V4L2 视频节点。
5. 当前板端已经具备从 RAW10 Sensor 数据到 NV12 用户态采集的完整链路证据。

## 需要纠正的操作

你输入的命令少了结尾的单引号：

```bash
dmesg | grep -Ei 'imx415|rkisp|csi|v4l2|media|camera'
```

之前的命令因此进入了 Shell 的续行状态，后面输入的内容没有按预期执行，最后通过 `Ctrl+C` 退出。这不是板子故障，是命令输入问题。

## Day 1 补充动作

只需要重新执行下面两条，并把完整输出发来：

```bash
dmesg | grep -Ei 'imx415|rkisp|csi|v4l2|media|camera'
v4l2-ctl -d /dev/video0 --list-formats-ext
```

补充完成后进入 Day 2：Sensor 上电、I2C、时钟、复位和寄存器初始化链路。

## dmesg 补充分析

本次日志已补齐，Day 1 的日志证据通过。

### 已确认的正常链路

- `rockchip-csi2-dphy ... probe successfully`：CSI-2 D-PHY 驱动初始化成功。
- `rkisp driver version: v01.08.00`：RKISP 驱动已经加载。
- `imx415 4-001a-1: driver version: 00.01.06`：IMX415 驱动实例在 I2C4、地址 `0x1a` 注册。
- `Detected imx415 id 0000e0`：成功读取到 Sensor ID，I2C 通信和基本识别成功。
- `dphy0 matches m00_b_imx415 ... bus type 4`：CSI-2 D-PHY 找到了 IMX415，并确认了总线类型。
- `Async subdev notifier completed`：Sensor、CSI 和 ISP 的异步子设备绑定完成。
- `set fmt: cur_mode: 3864x2192, hdr: 0`：当前使用 3864×2192、非 HDR 模式。
- `set exposure(shr0) ...`：驱动已经开始设置曝光寄存器，说明不仅完成 probe，还进入了工作配置阶段。

### 需要理解但暂不判定为故障的提示

- `Get hdr mode failed! no hdr default`：读取 HDR 模式失败后采用默认非 HDR 模式。当前 `hdr: 0` 且已有图像链路，因此暂不作为故障；面试时应说明 HDR 是可选模式，不等于 Sensor 没有被识别。
- `could not get sleep pinstate`：设备树中没有找到 sleep pinctrl 状态。当前系统仍能完成初始化，但说明设备树电源/引脚低功耗描述可能不完整。
- `Entity type ... was not initialized`：RKISP 旧内核/驱动组合中的 Media Entity 初始化提示。由于后续 Media Graph 已成功建立，当前先记录，不直接认定为阻塞问题。
- `max input:0x0@0fps`：RKISP 初始化早期尚未绑定输入 Sensor，后续异步绑定完成，不应单独据此判断无图像。

### 需要放入 Day 2 的核查项

日志中三组供电属性没有在设备树节点中找到：

```text
dvdd-supply ... failed
dovdd-supply ... failed
avdd-supply ... failed
using dummy regulator
```

这表示驱动没有从设备树获取对应的真实 regulator，而是使用了 dummy regulator。由于你的板子目前能够识别 Sensor 并出图，硬件供电大概率由模组或板级固定电源提供，但设备树的电源描述是否完整仍需核查。后续不能直接把它改成真实 regulator，必须先对照板卡原理图、Camera 模组手册和当前设备树确认。

## 面试表达练习

可以先这样表述本次实板结果：

> 我在 RK3568 上使用 IMX415 进行 Camera 链路验证。通过 Media Controller 可以看到 Sensor 挂在 I2C4、地址 0x1a，经 rockchip CSI-2 D-PHY、CSI 子设备和 RKISP ISP，最终连接到 rkisp_mainpath 的 `/dev/video0`。Sensor 侧输出 RAW10 Bayer，主路径输出 3840×2160 NV12，说明 Sensor、MIPI、CSI、ISP 和 V4L2 视频节点之间的链路已经建立。

这段话目前是“板端事实表达”，后续还要补充你对驱动代码、设备树和故障定位的理解。
