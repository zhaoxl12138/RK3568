---
id: EVID-20260728-IMX415-RUNTIME
doc-type: evidence
title: 直连板端读取 IMX415 运行时配置
course-stage: "02"
learning-status: validated
evidence-status: verified
publish-status: published
collected-at: 2026-07-28 (具体时分未记录)
board: ATK-DLRK3568 + IMX415
system: Buildroot (release string 未保留)
kernel: 4.19.232
sdk-commit: 未记录
dtb: 运行中 DTB 文件名与哈希未记录
device-ip: 192.168.0.230
raw-output-file: 无独立原始文件；关键输出摘录保存在本文
updated: 2026-08-10
---

# 2026-07-28｜直连板端读取 IMX415 运行时配置

## 环境快照

| 字段 | 采集当时的值 | 审计说明 |
|---|---|---|
| 采集时间 | 2026-07-28，具体时分未记录 | 不补造时间 |
| 板卡 / Sensor | `ATK-DLRK3568` / IMX415 | 运行时设备与驱动名称可交叉核对 |
| 系统 / 内核 | Buildroot / `Linux 4.19.232 aarch64` | 本记录不采用未绑定环境的 `4.19.255` |
| DTB / SDK commit | 未记录 | 运行时路径不能唯一确定源码提交 |
| 设备 IP | `192.168.0.230` | 只代表 2026-07-28 的 SSH 环境 |
| 原始输出文件 | 未单独保存 | 关键值以内嵌摘录保留 |

## 操作目的

从板端运行时 sysfs 和 device tree 树确认 IMX415 节点、endpoint、I2C client、驱动绑定、V4L2 subdev 与内核配置之间的关系。

## 原始命令

```bash
uname -a
find /sys/firmware/devicetree/base/i2c@fe5d0000/imx415@1a -maxdepth 2 -type f
grep -aH . /sys/firmware/devicetree/base/i2c@fe5d0000/imx415@1a/compatible
grep -aH . /sys/firmware/devicetree/base/i2c@fe5d0000/imx415@1a/status
readlink -f /sys/bus/i2c/devices/4-001a-1/driver
cat /sys/bus/i2c/devices/4-001a-1/name
cat /sys/bus/i2c/devices/4-001a-1/modalias
cat /sys/class/video4linux/v4l-subdev3/name
grep -E 'CONFIG_MEDIA_CONTROLLER|CONFIG_VIDEO_IMX415|CONFIG_VIDEO_ROCKCHIP_ISP' /proc/config.gz
```

命令按 Buildroot 可用工具记录，文本筛选使用 `grep`，不假设板端存在 `rg`。`/proc/config.gz` 的实际读取通常还需 `zcat`；原始会话没有保存完整命令行，故这里只保留当时记录的查询意图，不声称逐字复现。

## 原始输出

```text
/sys/firmware/devicetree/base/i2c@fe5d0000/imx415@1a
compatible: sony,imx415
status: okay
clock-names: xvclk
rockchip,camera-module-facing: back
rockchip,camera-module-lens-name: CS-P1150-IRC-8M-FAU
rockchip,camera-module-name: CMK-OT1522-FG3
data-lanes: 1 2 3 4
remote-endpoint: /csi2-dphy0/ports/port@0/endpoint@2

/sys/bus/i2c/devices/4-001a-1/name: imx415
modalias: of:Nimx415T<NULL>Csony,imx415
driver: /sys/bus/i2c/drivers/imx415
/sys/class/video4linux/v4l-subdev3: m00_b_imx415 4-001a-1

CONFIG_MEDIA_CONTROLLER=y
CONFIG_VIDEO_IMX415=y
CONFIG_VIDEO_ROCKCHIP_ISP1=y
CONFIG_VIDEO_ROCKCHIP_ISP=y
CONFIG_VIDEO_ROCKCHIP_ISP_VERSION_V1X=y
CONFIG_VIDEO_ROCKCHIP_ISP_VERSION_V21=y
```

当时还记录到运行时节点含 `reg`、`reset-gpios`、`power-gpios`、`clocks`、`power-domains`，但没有看到 `avdd-supply`、`dvdd-supply`、`dovdd-supply`。

## 可以证明

- `EVID-20260728-IMX415-RUNTIME` 可以证明本次启动环境存在 `sony,imx415` 的运行时节点，I2C client `4-001a-1` 已绑定到 `imx415` 驱动。
- Sensor endpoint 和 D-PHY endpoint 在运行时通过 phandle 关联，双方记录了 4 条 data lane。
- `CONFIG_VIDEO_IMX415=y` 与没有独立 `imx415.ko` 并不矛盾：该配置表示驱动内建。
- 设备 IP `192.168.0.230` 只用于追溯本次 SSH 环境；2026-05-28 的抓帧记录使用过 `192.168.0.103`，两者不是同一次环境快照。

## 不能证明

- 运行时节点存在不能证明仓库中哪一个 DTS/DTB 文件被加载，也不能证明对应 SDK commit。
- endpoint 与驱动绑定正常不能证明 MIPI 数据无误、ISP 已收到帧或用户态抓帧成功。
- 缺少 supply 属性与日志中的 dummy regulator 相符，但不能据此断言板级实际供电方式。
- 本记录不能为 Media API `4.19.255` 提供采集日期或运行内核归属；该值保持未核实。

## 关联课程阶段

- 主归属：`02` DTS 与设备发现
- 交叉引用：`03` IMX415 Sensor、`04` MIPI CSI-2 / D-PHY
- 环境基线：[[环境基线/RK3568-IMX415当前基线]]
- 配套日志与拓扑：[[2026-07-28-Camera驱动Day1验收]]

## 状态

`verified`
