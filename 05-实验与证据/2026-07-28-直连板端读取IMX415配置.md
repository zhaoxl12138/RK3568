# 2026-07-28｜直连板端读取 IMX415 配置

## 连接状态

- SSH 地址：`root@192.168.0.230`
- SSH 公钥登录已成功
- 板端内核：`Linux 4.19.232 aarch64`

## 当前运行设备树节点

```text
/sys/firmware/devicetree/base/i2c@fe5d0000/imx415@1a
```

已读取到：

```text
compatible: sony,imx415
status: okay
clock-names: xvclk
rockchip,camera-module-facing: back
rockchip,camera-module-lens-name: CS-P1150-IRC-8M-FAU
rockchip,camera-module-name: CMK-OT1522-FG3
```

二进制属性中还存在：

```text
reg
reset-gpios
power-gpios
clocks
power-domains
```

设备树中没有看到 `avdd-supply`、`dvdd-supply`、`dovdd-supply`，这与 dmesg 中的 `using dummy regulator` 对应。

## MIPI 端点

```text
data-lanes: 1 2 3 4
remote-endpoint: 已配置
```

说明当前 Sensor 使用 4 条 MIPI Data Lane，并通过设备树 endpoint 与 CSI 端连接。

## 驱动绑定证据

```text
/sys/bus/i2c/devices/4-001a-1/name       imx415
/sys/bus/i2c/devices/4-001a-1/modalias   of:Nimx415T<NULL>Csony,imx415
driver                                    ../../../../../bus/i2c/drivers/imx415
```

V4L2 子设备：

```text
/sys/class/video4linux/v4l-subdev3: m00_b_imx415 4-001a-1
```

主视频节点：

```text
/dev/video0 → rkisp_mainpath
driver      → rkisp
```

## 当前已证明的配置链

```text
设备树 imx415@1a
  compatible = sony,imx415
  reg = 0x1a
  data-lanes = 1 2 3 4
  remote-endpoint 已连接
        ↓
imx415 驱动绑定
        ↓
/dev/v4l-subdev3
        ↓
RKISP 驱动
        ↓
/dev/video0
```

## 当前下一步

下一步不是继续背 `media-ctl` 输出，而是读取 `compatible`、`reg`、`clock-names`、`reset-gpios`、`power-gpios`、`data-lanes` 各属性分别如何被驱动使用，再定位对应的设备树源码和 Sensor 驱动源码。

## 直连反查补充

### 驱动绑定

板端读取到：

```text
DRIVER=imx415
OF_NAME=imx415
OF_FULLNAME=/i2c@fe5d0000/imx415@1a
OF_COMPATIBLE_0=sony,imx415
MODALIAS=of:Nimx415T<NULL>Csony,imx415
```

驱动路径为：

```text
/sys/bus/i2c/drivers/imx415
```

说明 `compatible = "sony,imx415"` 已经匹配到内核中的 IMX415 驱动。

### endpoint 反向连接

IMX415 endpoint 的 `remote-endpoint` 指向：

```text
/csi2-dphy0/ports/port@0/endpoint@2
```

两端都配置了：

```text
data-lanes: 1 2 3 4
```

因此设备树中 Sensor 和 CSI-2 D-PHY 的连接不是靠名称猜出来的，而是靠两端 endpoint 的 phandle 互相指向建立的。

### 内核配置

当前内核配置确认：

```text
CONFIG_MEDIA_CONTROLLER=y
CONFIG_VIDEO_IMX415=y
CONFIG_VIDEO_ROCKCHIP_ISP1=y
CONFIG_VIDEO_ROCKCHIP_ISP=y
CONFIG_VIDEO_ROCKCHIP_ISP_VERSION_V1X=y
CONFIG_VIDEO_ROCKCHIP_ISP_VERSION_V21=y
```

`CONFIG_VIDEO_IMX415=y` 表示 IMX415 驱动直接编译进内核，不是单独的 `.ko` 模块。因此板端没有 `/sys/module/imx415` 目录，并不表示驱动不存在；它是 built-in 驱动。
