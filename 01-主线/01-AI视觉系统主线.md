# 01-AI视觉系统主线

本文件只做一件事：

```text
告诉我这整个 RK3568 AI Camera 项目现在走到哪一层了。
```

不负责讲命令，不负责读 PDF，不负责展开实验细节。

真正每天跟着走的文件是：

- [[02-从零到Python MVP学习路线|01-主线 / 从零到Python MVP学习路线]]

如果只记一句：

```text
01 看地图，02 真正执行，03 只在阶段6或复盘时回头看。
```

## 方向定位

目标不是转纯算法，也不是做大模型，而是把已有的 Camera、Linux、视频流经验升级成 AI 视觉系统工程能力。

岗位定位：

```text
懂 AI 部署的 Linux 多媒体工程师
```

系统闭环：

```text
Camera -> V4L2/GStreamer -> OpenCV -> RKNN/NCNN -> MIPI/RTSP/RTMP -> Linux部署
```

## 当前已跑通闭环

当前实际跑通的是 Python MVP：

```text
IMX415 MIPI Camera
-> RKISP / V4L2
-> GStreamer
-> OpenCV
-> YOLOv5 RKNN / NPU
-> MIPI屏显示
-> RTMP/HLS
-> Windows ffplay 拉流
```

## 阶段路线

- 阶段0：先看懂 AI Camera 链路
  当前状态：当前复盘起点
- 阶段1：Buildroot 验机
  当前状态：已完成
- 阶段2：Camera / V4L2 节点认知
  当前状态：已完成
- 阶段3：NV12 抓帧
  当前状态：已完成
- 阶段4：OpenCV + GStreamer
  当前状态：已完成
- 阶段5：RKNN 最小例程
  当前状态：已完成
- 阶段6：Camera + RKNN 汇合
  当前状态：已完成第一轮
- 阶段7：MIPI 屏显示
  当前状态：已完成
- 阶段8：RTMP/HLS 推流
  当前状态：已完成
- 阶段9：独立复现 Python MVP
  当前状态：第一轮阶段复盘完成后再做
- 阶段10：C++ 工程化
  当前状态：后置

## 当前优先级

```text
按 [[02-从零到Python MVP学习路线|01-主线 / 从零到Python MVP学习路线]] 从头复盘。
```

当前阅读顺序：

```text
阶段0 -> 阶段1 -> 阶段2 -> ... -> 阶段9
不要在这里来回跳专题。
```

现在不要做：

- 不切 Linux5.10。
- 不急着 YOLOv8。
- 不急着 RTSP。
- 不急着做 C++ RKNN API。
- 不继续堆临时脚本。

#主线 #AI视觉 #Camera #OpenCV #RKNN #HLS
