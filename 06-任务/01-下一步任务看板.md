# Camera 驱动当前任务

> 本页只维护“现在学到哪里”和“下一动作”。完整安排看 [[Camera驱动求职第1周执行计划]]，技术结论写回对应章节。

## 当前阶段

- 阶段：阶段 05：V4L2 Subdev
- 目标：沿 `imx415.c` 追清 I2C Driver、V4L2 subdev_ops、ctrl_ops 三类入口，并能解释 Sensor 如何被组装成 Camera 框架认识的 subdev
- 状态：进行中
- 最近复盘：2026-08-12

## 本轮唯一任务

- [ ] 完成 [[02-源码陪读/05-V4L2-Subdev/01-生命周期|驱动注册与 Subdev 组装]] 第一轮问答
- [ ] 指出 `imx415_i2c_driver`、`imx415_subdev_ops`、`imx415_ctrl_ops` 分别交给哪个框架
- [ ] 从 `v4l2_i2c_subdev_init()` 追到 `sd->ops = ops`，并说明挂接函数表不等于执行函数
- [ ] 从用户态 `STREAMON` 口述到 `imx415_s_stream()` 与 `__imx415_start_stream()`

## 已完成

- [x] 从原理图定位 IMX415 的 I2C、MCLK、RESET、PDN、电源和四条 MIPI Lane
- [x] 从 BoardConfig 找到实际 DTS
- [x] 解释 DTS 如何创建 I2C client，并通过 `compatible` 匹配 IMX415 驱动
- [x] 追到 `sensor_mod_init()`、I2C Core、`imx415_probe()` 和 V4L2 subdev 注册
- [x] 用板端输出验证运行时 DTS、`4-001a-1`、Sensor ID `0000e0` 和异步绑定
- [x] 完成阶段 04 D-PHY 的 HW/逻辑 probe、Endpoint/Async、Bound/Media Link 源码主线
- [x] 用 `media-ctl -p` 区分 entity、Source/Sink pad、`[ENABLED]` 与真实出帧证据

## 验收入口

- [[03-IMX415-Sensor-Bring-up|第 1 章技术内容与面试题]]
- [[04-MIPI-CSI2-DPHY|第 2 章学习记录]]
- [[02-源码陪读/05-V4L2-Subdev/00-源码陪读索引|阶段 05 源码陪读索引]]
- [[2026-07-28-直连板端读取IMX415配置|板端配置证据]]
- [[2026-07-28-Camera驱动Day1验收|Day 1 验收]]

## 暂不展开

- RKNN、YOLO、模型转换
- OpenCV 应用开发
- 显示和网络推流
- Linux 5.10 迁移
- Camera HAL、SERDES 和量产标定

#任务 #Camera驱动 #RK3568 #IMX415
