# 下一步任务看板

本文件只维护当前任务。完整历史见 [[每日进度记录]]，板子硬件验机事实见 [[板子到手验机记录]]。

## 待做

- [ ] 回看 AI 例程测试手册第 2 章和第 5 章，补齐 RKNN 运行库与 Lite2 概念
- [ ] 将拆分后的 SCRFD 脚本复制到板端，并复测单帧与 30 帧实时短测
- [ ] 设计 C++17/CMake 项目骨架

## 正在做

- [ ] 整理优先级最高：持续收敛 Python 实验目录，再继续跑新例程
- [ ] 固化规则：每成功一个功能，必须同步文档和产物索引

## 最近完成

### 2026-05-30

- [x] 将“每成功一个功能必须同步文档”写入当前阅读位置和 WSL 项目规则
- [x] 中文化 WSL 项目 README，并补充 OpenCV 实验 README
- [x] 创建代码私有仓库 `zhaoxl12138/rk3568_ai_camera`
- [x] 将 WSL 项目实验目录推送到 GitHub，默认分支为 `main`
- [x] 明确后续决策：整理优先级最高，不继续堆临时脚本
- [x] 建立 WSL 项目实验目录：`/home/rk3568/work/rk3568_ai_camera`
- [x] 整理 `opencv_camera` 和 `scrfd_face` 两个实验目录
- [x] 阅读 AI 例程测试手册第 6 章，明确 SCRFD 人脸检测例程目的和运行方式
- [x] 复制并改造 `11_facedet_scrfd_npu`，把 `cv2.VideoCapture(0)` 改成 GStreamer pipeline
- [x] 跑通 Camera 实时帧进入 RKNN 的人脸检测单帧例程
- [x] 修正图像方向后检测到 `faces 1`
- [x] 保存检测结果图：`E:\RK3568\face_stage\result\scrfd_result_rotated.jpg`
- [x] 新增 `main_gst_realtime.py` 并跑通 SCRFD 实时 90 帧测试
- [x] 输出实时统计：`processed_frames=90`、`fps=6.798`、`avg_faces_per_frame=0.889`
- [x] 保存实时结果图：`E:\RK3568\face_stage\result\scrfd_realtime_last.jpg`
- [x] 阅读并整理 SCRFD 单帧脚本与实时脚本的差异和复用点
- [x] 将 SCRFD 公共逻辑拆分为 `camera_gst.py` 和 `scrfd_detector.py`
- [x] WSL 本地完成 `py_compile` 语法检查
- [x] 整理 [[Camera-OpenCV-RKNN汇合路线]]
- [x] 判断官方实时 AI 例程优先级：优先 `11_facedet_scrfd_npu`，后置 YOLOv5/YOLOP
- [x] 备份板端旧 RKNN 运行库到 `/userdata/rknn_backup_1.3.0`
- [x] 将 `rknn_server/librknnrt.so` 从 1.3.0 更新到 1.5.0
- [x] 安装 `rknn-toolkit-lite2-1.5.0`
- [x] 跑通官方 `01_lenet` AI 例程，输出 `[5]: 1.0`
- [x] 阅读 OpenCV4 使用手册，确认出厂 Buildroot 已内置 OpenCV 4.5.5
- [x] 确认板端 Python `cv2` 可用
- [x] 确认 `cv2.VideoCapture(0)` 和强制 V4L2 后端无法直接打开 RKISP Camera
- [x] 使用显式 GStreamer pipeline 跑通 OpenCV 读取 `/dev/video0`
- [x] 生成并取回测试图片：`E:\RK3568\opencv_test\opencv_frame_gst.jpg`
- [x] 整理 OpenCV Camera Python 验证脚本到 WSL 项目实验目录
- [x] 连续抓取 30 帧 `1280x720 NV12`，文件大小 `41472000` 字节
- [x] 测试 `1920x1080 NV12` 单帧抓取，文件大小 `3110400` 字节
- [x] 确认 Camera 命令行采集链路稳定，可以进入 OpenCV 阶段

### 2026-05-28

- [x] 使用 Windows `ffplay` 正常查看 NV12 原始帧
- [x] 使用 `v4l2-ctl --stream-mmap` 从 `/dev/video0` 成功抓取一帧
- [x] 确认主采集节点：`/dev/video0` / `/dev/video-camera0`

#任务 #看板
